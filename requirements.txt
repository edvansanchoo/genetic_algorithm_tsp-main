# Desktop: pygame-ce is a drop-in replacement with wheels for Python 3.12+.
# Do NOT use `pygame` on Python 3.14 — it builds from source and fails.
pygame-ce>=2.5.0
matplotlib>=3.7.0
numpy>=1.24.0

# Web dashboard (python web.py)
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
websockets>=12.0
httpx>=0.27.0
markdown>=3.5.0
httpx>=0.27.0
markdown>=3.5.0
